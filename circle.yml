general:
  artifacts:
    - "wandbox-compilers.txt"

machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install requests
    - ./setup.sh

test:
  override:
    - python wandbox-listup-compiler.py > wandbox-compilers.txt
    