general:
  artifacts:
    - "wandbox-compilers.txt"

machine:
  python:
    version: pypy-2.2.1
  post:
    - pyenv global 2.7.9

dependencies:
  pre:
    - pip install requests
    - ./setup.sh

test:
  override:
    - python wandbox-listup-compiler.py > wandbox-compilers.txt
    